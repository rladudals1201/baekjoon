#include<iostream>
#include <set>
#include <algorithm>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;
    set<string> sets;

    for(int i = 0; i < n; i++) {
        string input;
        cin >> input;
        
        int len = input.length();
        char * chars = new char[len];
        for(int i = 0; i < n; i++) {
            
        }



    }


}